<!DOCTYPE html>


<html lang="{{ client.lang }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ t['page.description'] }}">
    <title>{{ t['page.title'] }}</title>
    
    <link rel="stylesheet" href="/static/css/fonts.css">
    <link rel="stylesheet" href="/static/css/base.css">
    {% if client.breakpoint == "phone" %}
    <link rel="stylesheet" href="/static/css/phone.css">
    {% elif client.breakpoint == "tablet" %}
    <link rel="stylesheet" href="/static/css/tablet.css">
    {% elif client.breakpoint == "screen" %}
    <link rel="stylesheet" href="/static/css/screen.css">
    {% else %}
    <link rel="stylesheet" href="/static/css/wide.css">
    {% endif %}
    
    {% block head %}{% endblock %}
</head>
<body>
    <header role="banner">
        <a href="/" class="logo" aria-label="{{ t['page.home'] }}">vivian voss</a>
        <nav role="navigation" aria-label="{{ t['nav.main'] }}">
            <a href="/" {% if current_page == 'index' %}aria-current="page"{% endif %}>{{ t["nav.home"] }}</a>
            <a href="/portfolio" {% if current_page == 'portfolio' %}aria-current="page"{% endif %}>{{ t["nav.portfolio"] }}</a>
            <a href="/knowledge" {% if current_page == 'knowledge' %}aria-current="page"{% endif %}>{{ t["nav.knowledge"] }}</a>
            <a href="/impressum" {% if current_page == 'impressum' %}aria-current="page"{% endif %}>{{ t["nav.impressum"] }}</a>
            {% for l in page.languages %}
                {% if l != client.lang %}
                    <a href="?lang={{ l }}" class="lang-switch" aria-label="{{ t['nav.switch_language'] }}" lang="{{ l }}">{{ l }}</a>
                {% endif %}
            {% endfor %}
        </nav>
    </header>
    
    <main>
        {% block content %}{% endblock %}
    </main>
    
    <footer>
        <p>&copy; {{ current_year }} vvoss.dev - {{ t["page.copyright"] }}</p>
    </footer>
    
    {% block scripts %}{% endblock %}
</body>
</html>